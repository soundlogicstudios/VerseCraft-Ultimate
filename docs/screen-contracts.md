# screen contracts (binding)
- screen css must only target its own root class
- screen js must mount/unmount cleanly
- background images are static; buttons are overlays
- body scrolling disabled; only explicit internal scroll regions may scroll
